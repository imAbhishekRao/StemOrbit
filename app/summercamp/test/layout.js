export const metadata = {
  title: "Summer Camp — banner test | STEMOrbit",
  description: "Internal preview: same Summer Camp page with a separate hero banner on small screens.",
  robots: { index: false, follow: false },
};

export default function SummerCampTestLayout({ children }) {
  return children;
}
